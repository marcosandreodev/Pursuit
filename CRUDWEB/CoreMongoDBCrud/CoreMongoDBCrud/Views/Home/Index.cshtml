@{
    ViewData["Title"] = "Employee Page";
}

<div class="row">
    <div class="col-md-4">
        <div class="row" style="display:none";>
            <div class="col-md-4">
                <label>Id :</label>
            </div>
            <div class="col-md-8">
                <input id="txtEmpId" class="inputField" style="width:100%;" placeholder="Id" />
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <label>Apelido :</label>
            </div>
            <div class="col-md-8">
                <input id="txtApelido" class="inputField" style="width:100%;" placeholder="Apelido" />
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <label>E-mail :</label>
            </div>
            <div class="col-md-8">
                <input id="txtEmail" class="inputField" style="width:100%;" placeholder="Email" />
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <label>Senha :</label>
            </div>
            <div class="col-md-8">
                <input id="txtSenha" class="inputField" style="width:100%;" placeholder="Senha" />
            </div>
        </div>
        <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-8">
                <button class="btn btn-primary" onclick="Save()">Save</button>
                <button class="btn btn-danger" onclick="Reset()" style="float:right;">Reset</button>
            </div>
        </div>
    </div>
    <div class="col-md-8">
        <table id="tblEmployee" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Employee Id</th>
                    <th>Apelido</th>
                    <th>E-mail</th>
                    <th>Senha</th>
                    <th>Ano</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>"


<script src="~/jquery/jquery.min.js"></script>
<script type="text/javascript">
    var _employees =[];
    $(document).ready(function () {
        LoadEmployees();
    });
    function LoadEmployees(){
        Reset();
        $.get("../Employees/GetEmployees", function (emps){
            _employees = emps;
            $.map(emps, function (emp) {
                var tempStr = "<tr>";
                tempStr += "<td>" + emp.id + "</td>";
                tempStr += "<td>" + emp.Apelido + "</td>";
                tempStr += "<td>" + emp.Email + "</td>";
                tempStr += "<td>" + emp.Senha + "</td>";
                tempStr += "<td>" + emp.id + "</td>";
                tempStr += "<td><button class='btn btn-primary' onclick='Edit(\"" + emp.id + "\")'>Edit</button><button class='btn btn-danger' onclick='Delete(\"" + emp.id + ""
            }
            
        });
    }
    function Reset(){
        _employees = [];
        $("#tblEmployee tbody tr").remove();
        $(".inputField").val("");
    }
    function Save(){
        var employee = {
            Id: $.trim($("#txtEmpId").val()),
            Apelido: $.trim($("#txtApelido").val()),
            Email: $.trim($("#txtEmail").val()),
            Senha: $.trim($("#txtSenha").val()),
        };
        $.post("../Employees/SaveEmp", employee, function (emp){

        });
    }
    function Edit(empId){
        var emp = _employees.find(x => x.id == empId);
        $("#txtEmpId").val(emp.id);
        $("#txtApelido").val(emp.apelido);
        $("#txtEmail").val(emp.email);
        $("#txtSenha").val(emp.senha);
    }
    function Delete(empId){
        $.ajax({
            url: "../Employees/DeleteEmp?empId=" + empId,
            method: 'DELETED'
        })
            .done(function (data) {
                alert(data);
                LoadEmployees();
            });
    }
</script>